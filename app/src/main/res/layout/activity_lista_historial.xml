<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#f8eef6"
    tools:context=".ListaHistorialActivity">

    <ImageButton
        android:id="@+id/btnAddHistorial"
        android:layout_width="@dimen/button_fab_size"
        android:layout_height="@dimen/button_fab_size"
        android:background="@android:color/transparent"
        android:src="@drawable/agregar"
        app:tint="#3d5a80"
        android:adjustViewBounds="true"
        android:scaleType="centerInside"
        android:contentDescription="@string/agregarHistorialDescripcion"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="@dimen/margin_l"
        android:layout_marginEnd="@dimen/margin_l" />

    <ScrollView
        android:id="@+id/scrollHistorial"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:padding="@dimen/margin_l"
        app:layout_constraintTop_toBottomOf="@id/btnAddHistorial"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TableLayout
            android:id="@+id/tableHistorial"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:stretchColumns="*" />
    </ScrollView>

</androidx.constraintlayout.widget.ConstraintLayout>
